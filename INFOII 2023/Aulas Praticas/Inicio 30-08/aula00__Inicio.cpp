//palavras chaves,
//tipos de erros, {logico, sintaxe}
//introducao a programas em c++
//variaveis
//tipos de dados
// sair
//inserindo a biblioteca
#include<iostream>
using namespace std;
int main(){
    // se data for maior q dia 30de agosto do ano 2027 pr	
	
	cout<<"Ola turm1"<<endl;
	cout<<"Ola turm2"<<endl<<endl<<endl;
	cout<<"Ola turm3"<<endl;

	return 0;
}


/*
Palavras-chave:

    As palavras-chave identificadas no código são #include, using, namespace, int, main, cout, endl, e return.

Tipos de erros:

    Não há erros de sintaxe evidentes no código.
    Um possível erro lógico pode ser o comentário mencionando uma condição que não está refletida no código.

Introdução a programas em C++:

    O código é um exemplo simples de um programa em C++. Ele usa a função main como ponto de entrada.
    O programa usa a biblioteca iostream para entrada e saída padrão.

Variáveis:

    Em C++, as variáveis são utilizadas para armazenar dados ou informações que podem ser usados no programa. Cada variável possui um tipo de dado que determina que tipo de valor ela pode armazenar (por exemplo, números inteiros, números de ponto flutuante, caracteres, etc.).

Tipos de dados:

 Em C++, existem diversos tipos de dados que você pode utilizar para armazenar diferentes tipos de valores. Cada tipo de dado tem um tamanho e um conjunto de valores que ele pode representar. Aqui estão alguns dos tipos de dados mais comuns em C++: int, float, double, long double, bool...

 ex: int idade = 30;
float altura = 1.75;
char letra = 'A';
bool esta_chovendo = false;
enum DiaDaSemana { DOMINGO, SEGUNDA, TERCA, QUARTA, QUINTA, SEXTA, SABADO };
DiaDaSemana hoje = QUARTA;



Sair:

    O programa usa return 0; para indicar uma saída bem-sucedida, onde 0 é um código de retorno padrão que indica sucesso. "voce tambem pode usar outro valor 10, 100"


Inserindo a biblioteca:

    A linha #include<iostream> é usada para incluir a biblioteca iostream, que fornece funcionalidades de entrada e saída padrão, como cout e cin.

Estrutura geral do programa:

    O programa começa com #include<iostream>, que inclui a biblioteca iostream.
    using namespace std; permite o uso do espaço de nomes std, que é onde estão definidas as classes e funções de entrada e saída padrão.
    A função main é o ponto de entrada do programa.
    O programa exibe várias mensagens na saída padrão usando cout.
    O programa retorna 0 para indicar que a execução foi bem-sucedida.

No entanto, o comentário sobre a data e a condição "se data for maior que 30 de agosto do ano 2027" não está refletido no código atual e pode ser um aspecto que precisa ser implementado ou esclarecido para que o código tenha um significado específico relacionado a essa condição.
Pelo exemplo da aula podemos fazer uma funcao que faz com que os nossos programas parem de funcionar ate uma determinada data.



*/
